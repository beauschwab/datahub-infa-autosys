When finishing a task:
- Run pytest -q if tests are relevant to changes.
- Ensure DataHub plugin entry points and recipes are updated when adding sources.
- Keep dataset-level lineage via UpstreamLineage; verify custom_properties patterns.
